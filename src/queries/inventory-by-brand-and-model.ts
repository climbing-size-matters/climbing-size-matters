// import { Cam } from '../cam-database/types';
// import { database } from '../cam-database/database';

//     function organizeInventoryByBrandAndModel (inventory: Cam[]) { //TODO: add output type
//         const organizedInventory = inventory.sort((a, b) => {
//             const brandComparison = a.brand_id.localeCompare(b.brand_id);
//             if (brandComparison !== 0) {
//                 return brandComparison; // If brands are different, use this result
//             }

//             // If brands are the same, compare by model
//             return a.model_id.localeCompare(b.model_id);
//         });
//     }

// export { organizeInventoryByBrandAndModel };
